= render layout: 'layouts/section', locals: { heading: 'New user', closeable: roster_users_path(@roster) } do
  = form_with model: [@roster, @user] do |f|
    = render partial: 'fields', locals: { f:, user: @user, path: roster_users_path(@roster) }
