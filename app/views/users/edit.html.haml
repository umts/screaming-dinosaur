= render layout: 'layouts/section',
         locals: { heading: 'Edit user',
                   closeable: Current.user.admin_in?(@roster) ? users_path : nil } do
  = render partial: 'form', locals: { user: @user, roster: @roster, path: user_path(@user) }
