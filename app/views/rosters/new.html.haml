%h1 New roster
%p
  To assign the fallback user, first create the roster,
  then add some users to it.

= form_for Roster.new do |f|
  .mb-3
    = f.label :name, class: 'form-label'
    = f.text_field :name, class: 'form-control'
  .mb-3
    = f.label :phone, class: 'form-label'
    = f.phone_field :phone, class: 'form-control'
  .mb-3
    = f.label :switchover_time, class: 'form-label'
    = f.time_field :switchover_time, class: 'form-control', value: '17:00', required: true
  = f.submit 'Save', class: 'btn btn-primary'
