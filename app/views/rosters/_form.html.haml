= form_with model: roster do |f|
  .mb-3
    = f.label :name, class: 'form-label'
    = f.text_field :name, required: true, class: 'form-control'
  .mb-3
    = f.label :phone, class: 'form-label'
    = f.phone_field :phone, class: 'form-control'
  .mb-3
    = f.label :fallback_user, class: 'form-label'
    = f.collection_select :fallback_user_id,
                          roster.users, :id, :full_name,
                          { include_blank: true },
                          disabled: roster.users.empty?,
                          class: 'form-select'
    - if roster.users.empty?
      .form-text To assign the fallback user, first create the roster, then add some users to it.
  .mb-3
    = f.label :switchover_time, class: 'form-label'
    = f.time_field :switchover_time, class: 'form-control', required: true
  = f.submit 'Save', class: 'btn btn-outline-primary w-100'
