:ruby
  locals = { path: roster_path(@roster),
             start_date: @start_date,
             end_date: @end_date,
             roster: @roster,
             assignment: @assignment,
             users: @users }
= render layout: 'layouts/section', locals: { heading: 'New assignment' } do
  = render partial: 'form', locals: locals
