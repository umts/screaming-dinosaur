{
  "name": "am-eve-split",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run start --env .env.dev",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "4.23.0",
    "@twilio/runtime-handler": "2.0.1"
  },
  "devDependencies": {
    "twilio-run": "^5.0.1"
  },
  "engines": {
    "node": "18"
  }
}
